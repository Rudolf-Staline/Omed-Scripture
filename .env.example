# Server-side API.Bible key used only by the Vercel function api/bible/chapter.ts.
# Do not prefix it with VITE_; VITE_* variables are exposed to the browser bundle.
BIBLE_API_KEY=

# Google OAuth client ID used for Google Sign-In and Drive AppData sync.
VITE_GOOGLE_CLIENT_ID=
