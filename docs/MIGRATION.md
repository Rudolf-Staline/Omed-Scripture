# Omed Scripture

Ce dépôt est le nouveau dépôt principal du projet.

Branche stable : `main`.

Ancien dépôt source : `Rudolf-Staline/Omed-Bible`.

Avant déploiement, vérifier :

```bash
npm install
npm run build
npm run lint
```
